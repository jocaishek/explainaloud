import { NotWiredYet } from "~/components/not-wired-yet";

export default function ReTeachPage() {
  return (
    <NotWiredYet
      title="Re-Teach isn't wired up yet"
      description="Once a concept gets flagged in a Gap Report, this is where you'd get a short, focused mini lesson on just that piece — not the whole topic again."
    />
  );
}
