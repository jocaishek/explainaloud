import { NotWiredYet } from "~/components/not-wired-yet";

export default function ReExplainPage() {
  return (
    <NotWiredYet
      title="Re-Explain isn't wired up yet"
      description="After a mini lesson, this is where you'd explain just the flagged pieces again, looping back to the Gap Report until nothing triggers a flag."
    />
  );
}
